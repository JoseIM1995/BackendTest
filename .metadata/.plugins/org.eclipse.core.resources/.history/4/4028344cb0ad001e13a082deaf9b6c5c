package com.test.application.entity;

import com.fasterxml.jackson.annotation.*;

@Entity
@Table(name = "GENDERS")
public class Gender {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Integer id;

    @Column(name = "NAME", nullable = false)
    private String name;

    @OneToMany(mappedBy = "gender")
    @JsonIgnore // Ignora la propiedad employees en la serialización
    private List<Employee> employees;

    // Resto de propiedades, getters y setters...

    @JsonProperty("id") // Nombre en JSON
    public Integer getGenderId() {
        return id;
    }

    public void setGenderId(Integer id) {
        this.id = id;
    }

    @JsonProperty("name") // Nombre en JSON
    public String getGenderName() {
        return name;
    }

    public void setGenderName(String name) {
        this.name = name;
    }

    // Resto de getters y setters...
}

